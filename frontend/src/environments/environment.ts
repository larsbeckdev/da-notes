// Produktions-Konfiguration.
// Wird beim Build (`ng build`) automatisch statt environment.development.ts
// verwendet, siehe fileReplacements in angular.json.
export const environment = {
  production: true,
  apiUrl: '/notes/',
};

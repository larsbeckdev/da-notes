// Production configuration.
// Used automatically instead of environment.development.ts during the build
// (`ng build`), see fileReplacements in angular.json.
export const environment = {
  production: true,
  apiUrl: '/notes/',
};

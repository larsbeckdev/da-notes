// Konfiguration für die lokale Entwicklung (`ng serve`).
// Der Port muss zu BACKEND_PORT aus der .env im Projektroot passen.
export const environment = {
  production: false,
  apiUrl: 'http://127.0.0.1:8300/notes/',
};
